## Packages
framer-motion | For smooth page transitions and UI animations
date-fns | For formatting timestamps in history
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- The backend api object is defined in @shared/routes
- Authentication is session-based via cookies
- Dark mode is default and mandatory for the gaming aesthetic
- Console output requires a monospaced font
